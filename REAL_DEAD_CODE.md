# Real Dead Code Analysis
Functions only used in tests (can remove function + tests):

## Orchestrator Methods:
- NewOrchestrator (only in tests)
- GetResources (only in tests)
- SyncLegacy (only in tests)
